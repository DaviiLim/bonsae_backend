/*

const mysql = require('mysql')

const connection = mysql.createConnection({
  host : 'localhost',
  port : '3306',
  user : 'root',
  password : 'admin',
  database : 'periodoLetivo'
})

connection.connect(() => {
  try{console.log('MONGO - A conexão foi um sucesso!!')}
  catch(error){
    console.log('MONGO - A conexão foi um fracasso!!')
  }
})

*/